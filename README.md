# Mediation Document Generator

This project generates a **Mediation Application Form (Form A)** in `.docx` format using Python.

## Requirements
- Python
- python-docx

## Run
```bash
pip install python-docx
python app.py
